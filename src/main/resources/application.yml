spring:
  datasource:
    url: jdbc:h2:mem:moodle_db
    username: admin
    password: admin
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console.enabled: true