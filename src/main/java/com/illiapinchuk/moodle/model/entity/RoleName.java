package com.illiapinchuk.moodle.model.entity;

/** Roles that can be used in Role. */
public enum RoleName {
  /** Default role. */
  USER,
  /** Teacher role. */
  TEACHER,
  /** Privilege role. */
  ADMIN
}
